# Workers

This folder contains worker stubs for OCR, face matching, and duplicate detection.
They are simple CLI programs that simulate processing and can be replaced by real ML code.

Usage:
```
python ocr_worker.py --input /path/to/file
python face_worker.py --input /path/to/file
python duplicate_worker.py --db /path/to/db
```
