# Mobile (Expo) — Placeholder

This folder contains a starter React Native (Expo) example with camera capture guidance logic.
It's a placeholder to integrate with the backend endpoints.

To create a full app:
1. Install Expo CLI: `npm install -g expo-cli`
2. `expo init mobile` and copy `App.js` contents below.
